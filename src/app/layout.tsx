import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
// @vercel/analytics/next integration - verified build: 23:01:40
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AVPL | Actual Virtual Protection Lab",
  description: "Administrative Portal for the Veermata Jijabai Technological Institute Protection Lab. Research and simulation infrastructure for modern power systems.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        {children}
        <Analytics />
      </body>
    </html>
  );
}
